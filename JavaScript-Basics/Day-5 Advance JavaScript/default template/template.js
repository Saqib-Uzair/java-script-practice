// Use backticks (``) to create strings with embedded expressions (${}):


const name = 'saqib uzair';
const age = 25;
const message = `My name is ${name} and I'm ${age} years old`;

console.log(message);